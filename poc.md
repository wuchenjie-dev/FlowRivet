# FlowRivet PoC 方案与验证记录

## 1. PoC 目标

验证 FlowRivet 能否通过 Codex、TAPD、飞书和 GitLab 建立从产品需求、功能规格、研发编码、测试到交付的可追溯闭环。

本阶段以 TAPD 为需求唯一事实源，以 GitLab 为代码与流水线事实源。飞书承载通知、讨论和评审协作，Codex 负责检查、编排、编码和闭环跟进，但不替代人工审批。

## 2. 环境隔离

| 环境 | 企业 | 项目 | 用途 | 权限策略 |
|---|---|---|---|---|
| 源环境 | 星网锐捷学科工具 | 阿在test，`56536239` | 读取现有 ABF 需求和验证真实字段差异 | 只读，不批量修改现有需求 |
| 沙箱环境 | FlowRivet 测试企业，`66238498` | FlowRivet Sandbox，`50396062` | 字段初始化、测试数据、状态流转和 E2E | 允许受控写入 |

企业 API 账号只能访问所属企业及明确授权的项目，不能跨企业访问源项目。因此 PoC 采用“源项目只读、沙箱项目写入”的双项目模式。

## 3. 凭证模型

FlowRivet 使用两类 TAPD 凭证，不能混用：

| 环境变量 | 认证方式 | 用途 |
|---|---|---|
| `TAPD_TOKEN` | Bearer Token | 日常用户态读取和允许的业务操作 |
| `TAPD_API_USER` | Basic Auth 用户名 | 企业管理员初始化 |
| `TAPD_API_PASSWORD` | Basic Auth 口令 | 企业管理员初始化 |
| `TAPD_SOURCE_WORKSPACE_ID` | 项目 ID | 源项目，只读 |
| `TAPD_SANDBOX_WORKSPACE_ID` | 项目 ID | 沙箱项目，允许受控写入 |

Windows 用户环境初始化示例：

```powershell
$token = Read-Host "TAPD 个人 Token"
$apiUser = Read-Host "TAPD API 账号"
$apiPassword = Read-Host "TAPD API 口令"

[Environment]::SetEnvironmentVariable("TAPD_TOKEN", $token, "User")
[Environment]::SetEnvironmentVariable("TAPD_API_USER", $apiUser, "User")
[Environment]::SetEnvironmentVariable("TAPD_API_PASSWORD", $apiPassword, "User")
[Environment]::SetEnvironmentVariable("TAPD_SOURCE_WORKSPACE_ID", "56536239", "User")
[Environment]::SetEnvironmentVariable("TAPD_SANDBOX_WORKSPACE_ID", "50396062", "User")
```

安装或初始化流程只能检查变量是否存在，禁止打印、记录或提交凭证值。

## 4. 已验证结果

验证日期：2026-07-31。

- TAPD 个人 Token 可以读取源项目，但不能创建自定义字段。
- TAPD 项目管理员权限不等于企业开放 API 配置写权限。
- 企业 API 账号通过 Basic Auth 认证，并能访问沙箱项目 `50396062`。
- 企业 API 账号不能跨企业访问源项目 `56536239`。
- 已在沙箱项目通过官方 API 创建并回读 14 个需求自定义字段。
- 14 个字段全部启用，字段类型和下拉候选值与设计一致。
- 创建逻辑按字段名称判断是否存在，可设计为重复执行不重复创建。
- 源项目现有需求没有被批量修改。
- 已实现 TAPD 需求分页读取、单需求读取和按字段名动态映射。
- 已实现确定性需求准入门禁，用户需求要求 VOC，其他类型接受对应替代证据。
- 已使用用户功能、技术架构和跨模块三类脱敏 ABF 样本验证批量评估链路。

## 5. 沙箱字段基线

| 逻辑字段 | TAPD 字段 | 类型 | 候选值 |
|---|---|---|---|
| 需求来源类型 | `custom_field_one` | 单选 | 用户VOC、产品规划、体验优化、技术、质量、安全合规、战略、探索 |
| 证据链接 | `custom_field_two` | 多行文本 | - |
| 目标用户 | `custom_field_three` | 多行文本 | - |
| 使用场景 | `custom_field_four` | 多行文本 | - |
| 当前问题 | `custom_field_five` | 多行文本 | - |
| 需求目标 | `custom_field_six` | 多行文本 | - |
| 成功指标 | `custom_field_seven` | 多行文本 | - |
| 范围 | `custom_field_eight` | 多行文本 | - |
| 不做范围 | `custom_field_9` | 多行文本 | - |
| 产品负责人 | `custom_field_10` | 人员选择 | - |
| 研发负责人 | `custom_field_11` | 人员选择 | - |
| 测试负责人 | `custom_field_12` | 人员选择 | - |
| 阻断问题 | `custom_field_13` | 多行文本 | - |
| 门禁结果 | `custom_field_14` | 单选 | 未检查、通过、阻断、有条件通过 |

字段内部编号由每个 TAPD 项目分配。FlowRivet 不得硬编码 `custom_field_*`，初始化后必须按中文字段名回读并生成项目级映射。

## 6. 工序与准入、准出标准

| 工序 | 准入标准 | 准出标准 |
|---|---|---|
| 需求准入 | 来源明确；至少一项可追溯证据；目标用户、场景和问题已描述 | 证据可访问；目标、成功指标、范围和不做范围明确；三方负责人明确 |
| 需求澄清 | 准入通过；原始材料可访问；产品负责人可参与 | 关键术语和边界无歧义；阻断问题关闭；遗留问题有责任人和期限 |
| 需求拆分 | 目标和范围确认；主要场景和依赖可识别 | 子需求可独立实现、测试、验收，并可追溯到上级目标和证据 |
| 功能规格设计 | 需求边界、依赖、研发和测试负责人明确 | 正常与异常流程完整；规则、数据、接口和验收标准可执行、可验证 |
| 规格自检 | 功能规格初稿完成；必填字段完整 | 无明显冲突；验收标准可测试；阻断问题关闭 |
| 联合评审 | 自检通过；产品、研发、测试评审人明确 | 三方通过；阻断修改项关闭；遗留风险被明确接受 |
| 规格准出 | 联合评审通过；最新版本归档 | 规格形成基线；可创建研发任务和测试用例；变更进入受控流程 |
| 研发与测试交付 | 规格已准出；任务、负责人、仓库和计划明确 | 代码、测试、MR、CI 和验收证据完整关联，缺陷闭环 |

VOC 不是所有需求的强制条件。客户需求应提供 VOC；技术、质量、安全、战略和探索类需求可以分别使用故障数据、质量报告、审计结果、决策记录或实验假设作为替代证据。

## 7. FlowRivet 初始化流程

1. `doctor` 检查 Node、Codex、TAPD CLI、Git 和必要环境变量。
2. 使用个人 Token 验证用户身份和源项目只读权限。
3. 使用企业 API 账号验证 Basic Auth、企业归属和沙箱项目授权。
4. 拒绝源项目与沙箱项目 ID 相同的危险配置，除非显式批准生产模式。
5. 获取现有自定义字段，按字段名幂等创建缺失字段。
6. 回读字段类型、候选值和启用状态，生成项目级字段映射。
7. 读取工作流、状态和流转规则；官方 API 不支持的工作流写配置由模板复制或一次性 UI 配置完成。
8. 创建带 `FLOWRIVET_POC` 标签的测试需求，不使用真实客户数据。
9. 默认以 dry-run 执行门禁、提醒和同步。
10. 所有检查通过后，才允许逐项开启 TAPD、飞书或 GitLab 写操作。

## 8. E2E 验证矩阵

| 场景 | 操作 | 预期结果 |
|---|---|---|
| 正常需求准入 | 创建证据和必填信息完整的沙箱需求 | 门禁通过，可进入下一工序 |
| VOC 缺失 | 创建用户需求但不提供 VOC | 返回阻断，不允许准入 |
| 替代证据 | 创建技术需求并提供故障或性能数据 | 不因缺少 VOC 被误拒 |
| 字段缺失 | 删除成功指标或负责人 | 返回稳定问题码和补充动作 |
| 重复初始化 | 连续运行两次初始化 | 第二次全部跳过，不产生重复字段 |
| 身份映射 | 映射 TAPD 用户与飞书用户 | 只接受稳定 ID 或唯一且经确认的邮箱映射 |
| 映射冲突 | 构造重名、一对多或离职用户 | 安全失败，不发送提醒 |
| 飞书 dry-run | 生成角色提醒 | 生成预览但不发送消息 |
| 飞书实发 | 向白名单测试群发送一次 | 卡片可跳回正确 TAPD 需求，重跑不重复发送 |
| GitLab 追溯 | 关联分支、Commit、MR 和 CI | 可从需求追溯到代码和测试证据 |
| 接口故障 | 模拟超时、429、401 和 403 | 瞬时错误有限重试，权限错误立即失败并告警 |
| 越权保护 | 尝试向源项目写入 | 默认拒绝，并留下审计记录 |

## 9. PoC 准出标准

- TAPD 始终是需求唯一事实源，飞书不保存冲突状态。
- 沙箱初始化可重复执行，字段映射不依赖固定编号。
- 准入检查正确区分 VOC 和替代证据。
- 产品、研发、测试三类任务和责任人分派准确。
- TAPD 与飞书身份映射不按姓名自动猜测。
- 通知默认 dry-run，实发具备白名单、幂等、限流和审计。
- 需求、规格、任务、测试、缺陷、MR 和 CI 可端到端追溯。
- 超时、限流、重复事件、权限不足和未知用户能够恢复或安全失败。
- 自动化测试和真实沙箱 E2E 均通过。
- 产品、研发、测试共同形成书面 Go/No-Go 结论。

## 10. 下一步

1. 已完成字段初始化 CLI、`doctor`、dry-run、需求读取和准入门禁。
2. 在沙箱项目一次性配置或复制标准工作流。
3. 使用企业 API 账号创建三类沙箱需求，补齐真实 TAPD 门禁 E2E。
4. 建立 TAPD 与飞书测试用户映射，验证提醒、去重和审计。
5. 接入 GitLab 测试仓库，验证需求到 MR、CI 和验收的追溯。
6. PoC 通过后，再申请源企业 API 账号并制定全量 ABF 迁移方案。
